// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Modo servidor: habilita server-side rendering para API endpoints
  output: 'server',
});
